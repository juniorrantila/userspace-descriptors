userspace_descriptors_lib = library('userspace-descriptors', [
  'descriptor.c'
])

userspace_descriptors_dep = declare_dependency(
  link_with: userspace_descriptors_lib,
  include_directories: '..'
)
